# This file is deprecated in favor of pyproject.toml with uv
# 
# For installation, please use:
#   uv sync
#
# For development:
#   uv sync --extra dev
#
# For testing:
#   uv sync --extra test
#
# For all extras:
#   uv sync --all-extras
#
# If you must use pip, install from pyproject.toml:
#   pip install -e .

# Legacy dependencies (use pyproject.toml instead)
fastmcp>=0.2.0
boto3>=1.26.0
requests>=2.28.0
pydantic>=2.0.0
